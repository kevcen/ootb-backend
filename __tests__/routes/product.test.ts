import request from "supertest";
import app from "../../app";

// describe("Products", function () {
//   it("Attempts to get all products linked to all given categories", function (done) {
//     request(app)
//       .post("/products")
//       .send({ name: "basic" })
//       .expect([], done);
//   });
// });
